[build]
  publish = "dist"
  command = "npm run build"

[functions]
  directory = "netlify/functions"

# Scheduled function - runs every minute to check for alerts
[functions."scheduled-alerts"]
  schedule = "* * * * *"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
